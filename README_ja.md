# Redmine Pinned Issues Plugin

[English version / 英語版はこちら](README.md)

## 概要

このプラグインは、Redmineのチケット一覧画面で、特定のチケットを「ピン留め」して常に上部に表示させる機能を追加します。Twitterの固定ツイートのように、重要なチケットをチームメンバーに周知させるのに役立ちます。

さらに、個々のコメントを「重要」としてマークする機能も備えています。コメントが数十件に膨れ上がったチケットでも、本当に読ませたい1件が埋もれません。

有効期限の設定や、ロールベースの権限管理、プロジェクトごとの機能ON/OFFにも対応しており、柔軟な運用が可能です。

## スクリーンショット

ピン留めしたチケットは、ソート順に関わらず一覧の最上部に固定されます（優先度による色分けは任意機能）:

![チケット一覧](https://assets.st-note.com/img/1779545358-dWilhc2nvH56kMXyuPS01pA4.png)

重要登録したコメントはハイライト表示され、専用タブで絞り込めます:

![重要コメントタブ](https://assets.st-note.com/img/1787060623-UDaKsrNjA8LnOkJSzolCQGmb.png)

## 主な機能

### チケットのピン留め

* **チケットのピン留め/解除**: 特定のチケットを一覧の上部に固定したり、それを解除したりできます。
* **優先表示**: ピン留めされたチケットは、ユーザーがどのようなソート順を選択していても、常に一覧の最上部にまとめて表示されます。
* **有効期限**: ピン留めに有効期限を9種類のプリセット（30分、1/4/8時間、1/3日、1週間、1ヶ月、無期限）から設定でき、期限が来ると自動的にピン留めが非表示になります。
* **ピン留め内の並び順**: ピン留めされたチケット間では、無期限のものを最上部に、残り時間が長いものから順に表示。
* **権限管理**: 「チケットをピン留め」権限を特定のロールにのみ付与できます。
* **プロジェクトモジュール**: プロジェクトごとに、ピン留め機能自体を有効化/無効化できます。
* **視覚的インジケータ**:
  * ピン留めされた行は背景色でハイライト（奇数行/偶数行で色を分けて設定可能）。
  * チケットの題名前に 📌 アイコンを表示（一覧画面・詳細画面の両方）。
  * アイコンにマウスを合わせると、ピン留めした人と残り時間を表示。
* **右クリックメニュー操作**: すべての操作は右クリックメニューから実行でき、期限選択はサブメニュー形式。画面遷移やモーダルダイアログなしに、Ajaxでその場で完了。

### 重要コメント

* **コメントの重要登録**: 各コメントに表示される星ボタンで、重要状態をAjaxで切り替えられます。
* **ハイライト表示**: 重要登録されたコメントは、ヘッダ下のライン・本文の背景色・左側のラインで強調されます。
* **専用タブ**: チケット履歴に「重要コメント」タブが追加され、重要登録されたコメントだけに絞り込めます。1件も登録がない場合、タブは表示されません。
* **履歴への記録**: 重要登録・解除の操作はチケット履歴に記録され、該当コメントへのリンクが残ります。
* **権限管理**: 「コメントを重要化」権限を特定のロールにのみ付与できます。専用のプロジェクトモジュールは不要で、コアの「チケットトラッキング」モジュールに属します。

### チケット一覧の色分け（任意）

* 優先度・期限超過に応じてチケット行を色分けします。配色はFarend fancyテーマと同等です。
* デフォルトテーマのまま動作し、テーマの追加インストールは不要です。
* 初期値はOFF。プラグイン設定画面から切り替えられます。

## 動作要件

* Redmine 6.0 / 6.1 / 7.0
* PostgreSQL 16（他のDBも動作する可能性はありますが、公式にはテストしていません）

## インストール手順

1. Redmineのpluginsディレクトリに移動してclone します

   ```bash
   cd /path/to/redmine/plugins
   git clone https://github.com/seraph3000/redmine_pinned_issues.git
   ```
2. Redmineのルートディレクトリに移動し、以下のコマンドを実行してデータベースのマイグレーションを行います。

   ```bash
   # 本番環境の場合
   bundle exec rake redmine:plugins:migrate RAILS_ENV=production
   ```
3. RedmineのWebサーバーを再起動します。

   ```bash
   systemctl restart httpd
   ```

## セットアップと設定

インストール後、機能を利用するためには以下の設定が必要です。

### 1. 権限の設定（Redmine管理者向け）

Redmineの管理者が、どのロールに操作を許可するかを設定します。

1. **[管理] > [ロールと権限]** に移動します。
2. 権限を与えたいロール（例：「開発者」「管理者」など）をクリックします。
3. 「チケットトラッキング」の項目内で、必要な権限にチェックを入れます。
   * **「チケットをピン留め」** — チケットのピン留め/解除
   * **「コメントを重要化」** — コメントの重要登録/解除
4. [保存] ボタンをクリックします。

### 2. モジュールの有効化（プロジェクト管理者向け）

各プロジェクトの管理者が、自分のプロジェクトでピン留め機能を有効にする必要があります。

1. 機能を有効にしたいプロジェクトに移動します。
2. **[設定] > [モジュール]** タブを開きます。
3. 一覧の中にある「**チケットのピン留め**」にチェックを入れます。
4. [保存] ボタンをクリックします。

重要コメント機能にこの手順は不要です。コアの「チケットトラッキング」モジュールに属するため、チケットが存在するプロジェクトでは常に利用できます。

### 3. 表示色のカスタマイズ（任意）

ハイライトの色は、プラグインの設定画面から変更できます。

1. **[管理] > [プラグイン]** に移動します。
2. Redmine Pinned Issues プラグインの「設定」をクリックします。
3. カラーピッカーまたは16進数コードで以下を調整します：
   * ピン留め行の背景色（奇数行）
   * ピン留め行の背景色（偶数行）
   * 重要コメントのヘッダ下ラインの色
   * 重要コメントの本文の背景色
   * 重要コメントの本文左ラインの色
4. 必要に応じて **「チケット一覧の色分けを有効にする」** をONにします。
5. [保存] ボタンをクリックします。各色の「デフォルトに戻す」で初期値に戻せます。

色は16進数（`#RGB` または `#RRGGBB`）で指定してください。それ以外の値はデフォルト色にフォールバックします。

## 利用方法

### チケットをピン留めする

1. チケット一覧画面で、ピン留めしたいチケットを右クリックします。
2. コンテキストメニューから「**このチケットをピン留め**」にマウスを合わせ、表示されるサブメニューから期間を選択します：
   * 無期限
   * 1ヶ月 / 1週間 / 3日 / 1日
   * 8時間 / 4時間 / 1時間 / 30分
3. 画面遷移なしで即座にピン留めされます。
4. 解除する場合は、再度右クリックして「**ピン留めを解除**」を選択します。

ピン留めされたチケットは題名前に 📌 アイコンが付き、行の背景色がハイライトされます。詳細画面でも同様に題名横にピンアイコンが表示されます。アイコンにマウスを合わせると、ピン留めした人と残り時間が表示されます。

### コメントを重要登録する

1. チケットを開き、強調したいコメントを探します。
2. コメントの操作アイコン列にある星ボタンをクリックします。
3. 画面遷移なしで即座にハイライトされ、履歴の上部に「重要コメント」タブが現れます。
4. 解除する場合は、再度星ボタンをクリックします。

対象は本文があるコメントのみです。属性変更のみの履歴には星ボタンは表示されません。

## Rakeタスク

メンテナンスや状況確認のためのRakeタスクを複数用意しています。

```bash
# 利用可能なタスク一覧を表示
bundle exec rake redmine:pinned_issues:help RAILS_ENV=production

# 現在のピン留め状況（合計/有効中/期限切れ件数）を表示
bundle exec rake redmine:pinned_issues:status RAILS_ENV=production

# 有効中のピン留め一覧を表示
bundle exec rake redmine:pinned_issues:list RAILS_ENV=production

# 期限切れのピン留めレコードをDBから削除
bundle exec rake redmine:pinned_issues:cleanup RAILS_ENV=production
```

### 定期クリーンアップ（推奨）

期限切れのピン留めは外れて通常のソート順の位置に戻りますが、DB上にはレコードが残り続けます。cronに以下を登録して定期的に物理削除することを推奨します。

```cron
# 毎日3:00に期限切れピン留めを削除
0 3 * * * cd /path/to/redmine && /usr/bin/flock -xn /tmp/redmine_pinned_issues_cleanup.lock -c 'RAILS_ENV=production bundle exec rake redmine:pinned_issues:cleanup'
```

### 更新履歴

v0.4.2 (2026-08-18)

* 1チケットあたりの重要コメント上限を追加。プラグイン設定画面から変更可能。
  初期値は 0（無制限）。
* 星ボタンをクリック後、数秒間クリックを受け付けないよう変更。
  意図しない連続操作を防止。
* 同一チケットに対する操作を直列化。複数タブからの同時操作で
  上限をすり抜けたり重複レコードが作られたりしないよう修正。
* 二重送信時にPostgreSQLでエラーになる問題を修正。
* 重要登録ボタンのアイコンを星（fav）から自作の感嘆符SVGスプライトに変更。

v0.4.1 (2026-08-18)

* showIssueHistoryImportant の重複定義を削除。

v0.4.0 (2026-08-12)

* 重要コメント機能を追加。
  星ボタンでコメントを重要登録でき、履歴上でハイライト表示されます。
  「重要コメント」タブで絞り込み表示が可能。登録・解除はチケット履歴に記録されます。
  新設の「コメントを重要化」権限で制御します。
* 重要コメント用の色設定を3種追加（ヘッダ下ライン / 本文背景 / 本文左ライン）。

v0.3.0 (2026-07-22)

* チケット一覧の色分け機能を追加（Farend fancyテーマ互換）。
  優先度・期限超過に応じたチケット行の色分けに対応。配色はFarend fancyテーマと同等。
  デフォルトテーマのまま動作し、テーマの追加インストールは不要。
  プラグイン設定画面からON/OFF切り替え可能（初期値はOFF）。
* Redmine 7.0 に対応。

v0.2.1 (2026-05-20)

* デバッグ用CSSクラス (patch-alive-test) がチケット行に残っていた問題を修正。

v0.2.0 (2026-04-18)

* 初回公開リリース。
* 右クリックメニューからチケットのピン留め/解除が可能。
* 有効期限を9種類のプリセット（30分〜無期限）から選択可能。
* ロールベースの権限管理とプロジェクト単位のモジュール有効化に対応。
* ピン留め行の背景色を奇数行/偶数行で個別にカスタマイズ可能。
* チケット一覧・詳細画面に📌アイコンを表示。
* ピン留め内の並び順：無期限を最上位、以降は残り時間が長い順で表示。
* メンテナンス用Rakeタスク（status / list / cleanup / help）を提供。
* スコープ付きアソシエーションによる期限切れピンの自動フィルタリング。

## 作成者

seraph3000 ([GitHub](https://github.com/seraph3000))

## ライセンス

MIT License
